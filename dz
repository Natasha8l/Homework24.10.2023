def averag(*args):
    total = 0
    count = 0
    for arg in args:
        if type(arg) in (int, float):
            total += arg
            count += 1
    if count == 0:
        return 0
    return total / count
print(averag(10, 20, 35, 46, 57))
