x=1
while x<=3 and x<4: #т.к. натуральное число минимум 1, то от х<1 я избавился. для того, чтобы while не тупил, приставку not сам логически преобразовал в такое лог. выражение
  x+=1
else:
  print(x)
